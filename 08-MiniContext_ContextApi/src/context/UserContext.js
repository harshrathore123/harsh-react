//In this only proper JavaScript code is present.
//In this we are creating a context and exporting it.

import React from "react";

//Now making for context then we will find under react

export const UserContext = React.createContext();
 

export default UserContext;